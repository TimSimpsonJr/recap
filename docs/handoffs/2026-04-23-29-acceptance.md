# Issue #29 Manual Acceptance Checklist

Covers scenarios that unit and integration tests can't exercise: real UIA trees, real browser DOM, extension permission reload. Run each scenario against a built daemon + reloaded extension.

## Preflight

- [ ] `pytest tests/test_roster.py tests/test_recorder_finalize.py tests/test_detector.py tests/test_daemon_server.py tests/test_extension_lockstep.py tests/test_unscheduled_enrichment_integration.py -v --override-ini="addopts="` passes
- [ ] Extension version in `extension/manifest.json` reads `"version": "1.1.0"`
- [ ] No mojibake in this file (all `--`, `->` are plain ASCII)

## Extension reload

- [ ] Load `extension/` as an unpacked extension in Chrome
- [ ] Chrome prompts for new host permissions: `meet.google.com`, `meeting.zoho.*`, `meeting.tranzpay.io`
- [ ] User accepts
- [ ] Extension badge shows `ON` (connected state)

## Core scenarios

1. **Spontaneous Zoom call with 2+ participants**
   - [ ] Open Zoom, start an instant meeting
   - [ ] Invite a second account/device, have it join
   - [ ] Daemon auto-records (zoom behavior=auto-record)
   - [ ] Wait ~45 seconds (to cover at least one 30s refresh cycle)
   - [ ] Stop the meeting (end call or close the Zoom window)
   - [ ] Open the resulting note; confirm frontmatter `participants:` contains both display names
   - [ ] Daemon log shows at least one `roster.merge source=zoom_uia_periodic` line (or equivalent `zoom_uia_periodic` trace)

2. **Spontaneous Google Meet call with participants panel open**
   - [ ] Navigate to `https://meet.google.com/new`, create a call
   - [ ] Click "People" to open the participants panel
   - [ ] Have a second account join
   - [ ] Daemon auto-starts recording (extension-triggered path via `/api/meeting-detected`)
   - [ ] Wait ~45 seconds
   - [ ] End the call or close the tab
   - [ ] Confirm frontmatter `participants:` contains both names

3. **Spontaneous Zoho / tranzpay call**
   - [ ] Start a call on `meeting.zoho.com` or `meeting.tranzpay.io`
   - [ ] Second participant joins
   - [ ] Daemon records; wait for a refresh cycle
   - [ ] Stop the call
   - [ ] Confirm frontmatter `participants:` populated

4. **Teams native client regression**
   - [ ] Start a Teams call in the native client (not via browser)
   - [ ] Confirm detection + recording as today
   - [ ] Verify frontmatter `participants:` still populated (Teams UIA one-shot path, same as pre-#29 behavior)
   - [ ] Sidecar should have been written at start time carrying the Teams roster; finalize at stop returns same list, no rewrite

5. **Teams-via-browser documented gap**
   - [ ] Join a Teams meeting via `teams.microsoft.com` in Chrome
   - [ ] Confirm recording starts (extension detection path still fires)
   - [ ] Confirm frontmatter `participants:` is empty -- this is the documented v1 gap
   - [ ] No errors in daemon or extension logs

## Edge cases

6. **Meet with participants panel NEVER opened**
   - [ ] Join a Meet call; do NOT click "People"
   - [ ] Confirm recording runs and completes
   - [ ] Confirm frontmatter `participants:` is empty (documented limitation)
   - [ ] No errors

7. **Browser tab closed mid-call**
   - [ ] Join a Meet call; record briefly
   - [ ] Close the tab without ending the call cleanly
   - [ ] Daemon detects tab closure, stops recording
   - [ ] Next alarm tick silently skips the dead tab (no log noise about failed sendMessage)

8. **Late joiner in Meet appears within ~30s**
   - [ ] Start a Meet call with yourself
   - [ ] After ~45 seconds, have a second account join
   - [ ] Within the next 30-second refresh tick, the late joiner appears in daemon logs
   - [ ] Final frontmatter contains the late joiner

9. **Zoom UIA periodic cadence**
   - [ ] During a Zoom recording, watch daemon debug logs
   - [ ] Confirm `extract_zoom_participants` is called roughly every 30 seconds, not every 3 seconds

10. **Daemon restart mid-recording (crash simulation)**
    - [ ] Start a Zoom recording
    - [ ] Let a few 30s cycles accumulate names (watch logs)
    - [ ] Kill the daemon process (simulating a crash)
    - [ ] Inspect the sidecar: initial participants preserved (empty for Zoom/browser at crash time, since no rewrite happens before stop)
    - [ ] In-memory roster is lost -- expected degradation, not a bug

11. **Roster filter rejection**
    - [ ] During an active recording, POST a mixed payload to `/api/meeting-participants-updated`:
          `{"tabId": <real_tab_id>, "participants": ["Alice", null, 42, "Bob"]}`
    - [ ] Response is `200 accepted`
    - [ ] Roster contains `Alice` and `Bob` only; debug log records that two non-string entries were dropped

12. **Filter truncates to 100**
    - [ ] POST a 150-entry payload to `/api/meeting-participants-updated`
    - [ ] Response is `200 accepted`
    - [ ] Daemon log has a warning about truncation

## Sign-off

- [ ] All core scenarios (1-5) pass on a real Zoom, Meet, Zoho or tranzpay, Teams native, Teams browser session
- [ ] No new errors in daemon log beyond expected debug entries
- [ ] Extension badge never drops to `offline` during refresh ticks
- [ ] Ready for PR review
